<div class="card w-96 bg-zinc-200 shadow-xl">
  <figure ><img src="{{post.Image }}" alt="affiche du film" class="text-red-800 " /></figure>
  <div class="card-body">
    <h2 class="card-title">{{post.title}}</h2>
    <p>{{ post.description}}</p>
    <div class="card-actions justify-end">
      <button class="btn btn-primary">regarder</button>
    </div>
  </div>
</div>
